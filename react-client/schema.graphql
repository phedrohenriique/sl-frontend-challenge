## this file can't be imported normally, the object needs to be passed inside the server instance

type Query {
  hello: String
  world: Int
  users: [User]
}

type User {
  id: ID
  name: String!
  email: String!
  phone: String!
  password: String!
  avatar: String!
}
